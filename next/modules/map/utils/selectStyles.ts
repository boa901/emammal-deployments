const selectStyles = {
  menuPortal: (base) => ({
    ...base,
    zIndex: 9999,
  }),
  control: (base) => ({
    ...base,
    maxHeight: '35px',
  }),
  valueContainer: (base) => ({
    ...base,
    maxHeight: '35px',
  }),
  indicatorsContainer: (base) => ({
    ...base,
    maxHeight: '35px',
  }),
};

export default selectStyles;
